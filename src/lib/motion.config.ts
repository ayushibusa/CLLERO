// Central animation settings for the website
export const EASE = [0.22, 1, 0.36, 1]; // Smooth ease-out-expo curve

export const DURATION = {
  micro: 0.15,   // For quick elements like button/icon hover
  fast: 0.25,    // For dropdowns, tooltips, tab changes
  base: 0.4,     // For card entries, panel transitions, modal open/close
  slow: 0.6,     // For page/section entries, large reveals
};
